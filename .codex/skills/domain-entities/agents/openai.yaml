interface:
  display_name: "Domain Entities"
  short_description: "Model domain entities and invariants"
  default_prompt: "Use $domain-entities to create or change a domain entity and its behavior."
