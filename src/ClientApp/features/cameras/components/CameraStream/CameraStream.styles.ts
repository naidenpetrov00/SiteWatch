import { StyleSheet } from "react-native";

export const cameraStreamStyles = StyleSheet.create({
  streamWrapper: {},
  video: {
    width: "100%",
    aspectRatio: 16 / 9,
  },
  streamLabel: {
    fontSize: 18,
  },
});
