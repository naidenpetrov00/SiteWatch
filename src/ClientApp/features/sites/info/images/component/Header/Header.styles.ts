import { StyleSheet } from "react-native";

export const imagesHeaderStyles = StyleSheet.create({
  title: {
    fontSize: 24,
    fontWeight: "700",
  },
  subtitle: {
    marginTop: 4,
    marginBottom: 20,
    fontSize: 14,
  },
});
